package com.helix.automation.framework.core;

/**
 * Placeholder class kept in main source to avoid a missing-class compile error when running
 * in environments that expect this symbol. The real TestNG listener implementation lives
 * in test sources so it does not add runtime dependency on TestNG to the main artifact.
 */
public class TestListener {
    // no-op placeholder — actual test listener is under src/test/java
}
